version: 1
schemas:
  - id: wd
    parent: root
    # Match note names like `wd.task.2026.some-task`, then apply the task
    # template to the task notes created beneath the year bucket.
    children:
      - pattern: "task"
        children:
          - pattern: "[0-2][0-9][0-9][0-9]"
            children:
              - pattern: "*"
                template: template.task
