---
id: waprogversionapi20260721
title: 'Programmatic version API — pass-bytes-and-designator, in-process'
desc: 'Cut 2026-07-21 13:22 by the Stagecraft flagship PM seat (cross-shop lane, Dave-authorized). The 2026-07-06 deferred pass-bytes programmatic `version` API; wake fired at the Stagecraft press panel. Deliverable: the API itself, spec''d and landed in weave.'
---

## Goals

- A stable, named, in-process Weave library API that lets a consumer say: **"here are payload bytes and a designator path (or payload IRI); record them"** — working-surface update and/or history versioning — with exactly the guarantees the CLI path gives (single-writer, fail-closed validation, snapshot verification, retry-safe no-op semantics), and **no subprocess spawn, no temp-file requirement on the caller**.
- The product-path law this serves (ruled, Stagecraft 2026-07-21): **consumer product paths call the API, never exec the CLI.** The standing counterexample: `weave` was absent from the consuming host's PATH until 2026-07-21 morning — CLI-spawn is a deployment coupling, not an API.

## Summary

Weave already versions payloads two-step: `weave payload update <file> <designatorPath>` replaces working bytes from a FILE source; `weave version --target designatorPath=...` records targeted working state into history via the version-plan machinery (`src/core/payload/update.ts`, `src/core/payload/version_intent.ts`, `src/core/weave/version_plan.ts`), with the CLI (`src/cli/run.ts`) owning invocation. `src/mod.ts` re-exports the core wholesale, so internals are *reachable* as a library today — but nothing is a *named, stable, documented* programmatic contract, and the file-source assumption in `payload update` forces callers to stage temp files.

This task was deferred on 2026-07-06 (Stagecraft disc "semantic-flow-in-the-platform" §6: no new API surface until a real trigger) and the wake fired 2026-07-21: the Stagecraft checkpoint press is the natural weave-version moment (publication cadence == version cadence; an unwoven pressed checkpoint is a second-class mesh state against SF-serialization doctrine), and the srd-rules publication estate now execs the CLI from its pipeline.

**Consumers, in ruled order:** (1) the `stagecraft/srd-rules` mesh estate's extraction/publication pipeline; (2) the Stagecraft checkpoint press — whose consumption is **explicitly not this task**: it folds into the Stagecraft press-completeness lane later, via the dependency-PIN idiom (full-commit pin + out-of-gate build + contract check). The deliverable here is the API itself, landed in weave.

## Discussion

- **Pass-bytes semantics vs the working-file capture contract.** The CLI's multi-target batch hashes the *working payload files* before batch capture and verifies after; a changed file refuses the whole batch. A bytes-in API changes what "the working surface" means at capture time: the API presumably (a) writes the passed bytes to the working payload file and (b) versions from that state — but whether (a)+(b) are one atomic composed call, two composable calls, or both-offered is the central spec question. The refusal/verification semantics must be restated precisely for the bytes-in path — nothing weakens.
- **The mesh stays file-backed.** Pass-bytes means the CALLER passes bytes; the mesh on disk remains the truth the API mutates. This is not an in-memory mesh.
- **Single-writer enforcement in-process.** The CLI's single-writer guarantee is process-shaped today. A library caller (long-lived daemon like the Stagecraft session authority, or a pipeline script) needs a stated concurrency contract: what serializes, what refuses, what happens on concurrent CLI use of the same mesh.
- **Return contract.** Callers need machine-usable results: the applied version-plan (created/updated files, new state segment, the payload artifact IRI, the current-history path), and typed refusals. `VersionPlan`/`PayloadVersionIntentPlan` are the natural seeds.
- **Behavior-spec law.** This is externally visible behavior spanning subsystems — per `wd.general-guidance`, an `sf.spec.*` note in semantic-flow-framework should ride the landing (proposed; spec review confirms scope).
- **Known 0.3.0 operational defects are SEPARATE work** (boarded on the Stagecraft side after the srd-rules phase-A run): `extract --all-terms` file-URL crash; extracted-term Knops missing `sflo:hasResourcePage` claims; whole-mesh `validate` heap exhaustion at ~6,900 files. Not in scope here unless the spec round proves one trivially adjacent — say so explicitly if so.

## Open Issues

*(for the spec review to attack; proposed leans in bold)*

- [ ] API shape: one composed `version(bytes, designator, opts)` vs composable `updatePayload(bytes, designator)` + `version(targets, opts)` pair. **Lean: the composable pair, mirroring the CLI's proven two-step, plus a thin composed convenience.**
- [ ] Batch surface: does the v1 API carry multi-target batches with the same all-or-nothing refusal, or single-target only? **Lean: mirror the CLI batch contract — the press's checkpoint moment is inherently multi-target.**
- [ ] Naming/segments: how `historySegment`/`stateSegment`/`manifestationSegment` and mesh-config defaults surface in the API signature. **Lean: one options object mirroring the CLI's precedence rules exactly.**
- [ ] Error taxonomy: typed refusals vs thrown errors; what the CLI's diagnostics become programmatically.
- [ ] The stable-surface boundary: what gets NAMED in `mod.ts` as the public API vs what stays incidental export. **Lean: a dedicated `src/api/` (or `core/api/`) module with an explicit export list; the wholesale re-export is not a contract.**
- [ ] Binary payloads: the press moment includes non-text artifacts (`PlannedBinaryFile` exists) — bytes-in must state its encoding contract (Uint8Array; no string-only API).
- [ ] `sf.spec.*` note: which repo lands it and whether it gates this task's close or rides as a fast-follow.
- [ ] Version/release: does this ship as 0.4.0? (`deno task bump:version`; release-note convention per `wd.release-runbook`.)

## Decisions

- **2026-07-21 (ruled at cut, Dave-concurred relay):** the task lives in the weave world under weave's own conventions; the Stagecraft lab keeps only a pointer. The portable seat-separated lifecycle runs here exactly as in the Stagecraft shop (detached spec review → adjudication on this note → build seat in a weave working clone → close review).
- **2026-07-21 (product-path law, standing):** consumers call the API; the CLI is for humans and scripts, never a product path's mechanism.
- **2026-07-21 (consumer order):** srd-rules estate first, Stagecraft press later via its own lane; press-side consumption is out of scope here.

## Contract Changes

- New named public programmatic API surface (module + explicit exports), documented in `wd.*` (developer) and referenced from the CLI reference notes where the CLI and API share semantics.
- No CLI behavior change; no mesh-format change; no daemon/HTTP surface.
- Docs: `wd.codebase-overview` updated before merge (standing law); release note entry.

## Testing

- Deno-native (`deno task test` / `check` / `lint`; `deno task fmt` + `deno task ci` before merge) per [[wd.testing]].
- **Equivalence proof (the load-bearing family):** on a fixture mesh, the API path and the CLI path produce byte-identical version output for the same inputs (single and batch), including the no-op re-run and the refusal families (malformed target, changed-under-capture, malformed batch member refusing the whole plan).
- Fail-on-old probes: each preserved invariant gets a test that demonstrably fails without the guard.
- Binary payload round-trip.
- No consumer-side (Stagecraft) tests in this task.

## Non-Goals

- Press-side consumption wiring (Stagecraft press-completeness lane owns it, later, via the PIN idiom).
- Node packaging / consumer build machinery / JSR publication decisions beyond what landing the API in this repo requires.
- CLI retirement or change; daemon/HTTP API; in-memory mesh.
- The boarded 0.3.0 defect trio (all-terms crash, missing page-claims, whole-mesh validate heap) — separately tracked.

## Implementation Plan

*(draft — the build seat refines after the spec round adjudicates)*

- [ ] Spec review r1 (detached seat, adversarial) → adjudication on this note → amend until converged.
- [ ] Carve the named API module + explicit exports; restate capture/refusal semantics for bytes-in.
- [ ] Implement composable calls (+ composed convenience if ruled); typed results/refusals.
- [ ] Equivalence + refusal + no-op + binary test families; fail-on-old probes.
- [ ] Docs (`wd.codebase-overview`, API doc note, CLI-reference cross-links); `sf.spec.*` per the ruled scope.
- [ ] `deno task fmt` + `deno task ci`; release-note entry; close review (fresh seat) → adjudication → land.

## Spec review

*(r1 appends here — PROPOSED verdicts/severities; execution blocked until PM adjudication signs off.)*

### r1 — adversarial spec review (2026-07-21)

All findings and verdicts in this review are PROPOSED. The PM adjudicates them on this note.

#### Ranked findings

1. **PROPOSED BLOCKER — The public operation and its bytes-capture boundary are not decidable.** The goal alternates among working-surface update, history versioning, and “update and/or history versioning,” while the discussion leaves one composed call, two calls, or both open. These are observably different contracts. The CLI exposes two separately committing commands: `payload update` invokes `executePayloadUpdate` (`src/cli/run.ts:1100-1154`), while `version` independently invokes `executeVersion` (`src/cli/run.ts:291-375`). The update writes the working file before it returns (`src/runtime/payload/update.ts:96-105`); `executeVersion` accepts no bytes and prepares from file-backed state (`src/runtime/weave/weave.ts:217-251`). The note does not say when a passed `Uint8Array` is copied, whether later caller mutation is visible, what disk state is compared to what input, when the working file becomes visible, whether a version refusal leaves the new working bytes installed, or whether a post-capture working-file change is ignored in favor of the passed/captured bytes. It also does not define whether “snapshot verification” means preserving the CLI's exact multi-file hash window or establishing a new compare-and-swap precondition for bytes-in. A builder would have to invent all of these rules. **REQUIRED RESOLUTION:** Pin the v1 operations and phase diagram before build: update-only, version-current, and/or update-and-version; call-admission copy/ownership of every byte buffer; discovery and validation; the exact pre/post verification set and comparison; the first possible write; what remains changed after each refusal/error stage; and the authoritative bytes after capture. Include one normative concurrent-mutation example. Do not use “exactly the guarantees” until each guarantee is scoped to an operation and phase.

2. **PROPOSED BLOCKER — “Single-writer guarantee/enforcement” contradicts landed policy, and concurrent API + CLI behavior cannot be supplied without expanding scope.** Weave does not currently enforce a single writer. The service guidance says the service must act as the single writer and explicitly says Weave provides no locking, rollback, journaling, or filesystem transactions (`documentation/notes/wu.cli-reference.weave.md:71-77`). The durable decision is even more direct: the requesting application owns serialization, locking, rollback, and retry orchestration (`documentation/notes/wd.decision-log.md:23-30`). The draft instead calls the CLI guarantee “process-shaped” and asks what the library serializes or refuses. An in-process mutex would serialize only cooperating calls and cannot refuse a concurrent CLI. A cross-process mesh lock would have to be shared by the CLI/runtime, contradicting “No CLI behavior change” (`wa.task...programmatic-version-api.md:51`). **REQUIRED RESOLUTION:** Choose one contract explicitly: (a) retain caller-owned single-writer preconditions, state that concurrent API/API and API/CLI mutation is outside the supported contract except for the bounded snapshot guard, and remove claims of enforcement; or (b) expand the task to a shared cross-process lock/protocol used by API and CLI, including acquisition identity, stale-lock recovery, lock scope, refusal type, and compatibility behavior. The current wording cannot support both.

3. **PROPOSED BLOCKER — The binary promise is not a wrapper-sized change and currently has no CLI-equivalent path.** Core payload update accepts `replacementPayloadTurtle: string`, rejects blank text, and emits `PlannedFile` (`src/core/payload/update.ts:11-28`, `src/core/payload/update.ts:137-145`). Runtime update reads and writes text (`src/runtime/payload/update.ts:226-259`, `src/runtime/payload/update.ts:407-459`) and the settled-shape assertion requires the payload to be an `sflo:RdfDocument` with `_history001` (`src/core/payload/update.ts:165-193`). `PlannedBinaryFile` only establishes a binary *created-output* shape (`src/core/planned_file.ts:1-9`, `src/core/weave/version_plan.ts:3-9`). First payload versioning correctly emits a binary historical snapshot (`src/core/weave/weave.ts:1175-1237`), but later payload versioning always puts decoded `currentPayloadTurtle` in `createdFiles` (`src/core/weave/weave.ts:1682-1760`), even though the loader separately carries `currentPayloadBytes` for non-text extensions (`src/runtime/weave/artifact_loaders.ts:71-95`). Therefore “binary round-trip” and CLI/API equivalence cannot be satisfied by carving an API around the landed update/version seams; later binary advancement is directly adjacent correctness work. **REQUIRED RESOLUTION:** Either include end-to-end byte-preserving working update plus first/later history advancement in this task (and acknowledge the necessary shared runtime/CLI behavior correction), or narrow v1 explicitly to a defined UTF-8 text/RDF class and remove the binary/`Uint8Array` promise. If binary remains, specify zero-length policy, exact byte identity, input-buffer copying, extension/content-kind classification, invalid UTF-8 behavior for text/RDF paths, and first and later no-op comparisons.

4. **PROPOSED BLOCKER — Retry-safe no-op and snapshot verification are overgeneralized beyond their landed batch domain.** The runtime attempts the special payload batch only when there are more than one exact targets and overwrite is false (`src/runtime/weave/version_execution.ts:625-631`). Only that path hashes inputs and reloads settled payload candidates (`src/runtime/weave/version_execution.ts:136-201`). Batch planning skips already-current members by exact byte/text and requested-segment checks (`src/core/weave/weave.ts:574-580`, `src/core/weave/weave.ts:907-960`). A settled single exact target is not loaded as weaveable in the normal path (`src/runtime/weave/candidate_loader.ts:311-367`) and uncovered explicit targets refuse (`src/runtime/weave/candidate_loader.ts:38-64`); it does not inherit the batch no-op rule. The note's goal and “single and batch” equivalence family do not expose this distinction. **REQUIRED RESOLUTION:** Decide whether an API request containing one payload is a one-member coherent batch with snapshot verification and success/no-op, or mirrors current single-target `executeVersion` refusal behavior. Pin overwrite behavior, empty batch behavior, duplicate/aliased targets, and whether naming-only requests intentionally create a state rather than no-op. If retry-safe no-op is a stable API guarantee, make it an explicit per-input result state and test one-member as well as multi-member retries.

5. **PROPOSED HIGH — “Whole-batch refusal” is being conflated with transactionality across working updates and output writes.** Landed batch behavior validates a `VersionPlan` before version writes, but the durable decision calls it “not a transactional filesystem boundary” (`documentation/notes/wd.decision-log.md:23-30`). `writePreparedVersion` preflights paths/RDF, then writes text creates, binary creates, and updates in separate loops (`src/runtime/weave/version_execution.ts:868-933`, `src/runtime/weave/version_execution.ts:997-1021`), so an I/O failure can leave partial output. With the proposed pair, one or more working updates can also commit before version planning refuses. The CLI docs' retry statement does not define recovery from every possible mid-target partial write. **REQUIRED RESOLUTION:** Define three separate guarantees: whole-request semantic planning/refusal before *version-output* writes; working-surface mutation behavior; and non-transactional I/O failure behavior. State whether the composed API plans from an in-memory byte overlay before changing working files, and whether a validation refusal preserves old working bytes or installs the requested bytes. Add write-failure injection cases at working-update, text-create, binary-create, and support-update phases; do not label the filesystem result “all-or-nothing” unless a real transaction/rollback mechanism is added.

6. **PROPOSED HIGH — Target identity, mesh identity, and source ownership are unnamed.** Existing public requests accept a designator path, not a payload IRI (`src/core/targeting.ts:6-15`, `src/core/weave/requests.ts:16-19`), while the goal promises either. The note gives no `meshRoot` type/default, no path-versus-IRI discriminated union, no rule for both values, no requirement that an IRI be under the loaded mesh base, no percent-encoding/root-designator rule, and no duplicate identity rule after normalization. More seriously, runtime version loading prioritizes a repository-source floating locator over the mesh-relative working path (`src/runtime/weave/artifact_loaders.ts:76-86`), while payload update writes the mesh-relative working file under the workspace (`src/runtime/payload/update.ts:330-367`). A naïve composed API can therefore update one file and version another. **REQUIRED RESOLUTION:** Pin the request identity schema and normalization/refusal rules, including `meshRoot`; designator path versus payload IRI; root; percent encoding; outside-mesh IRIs; both/neither; duplicate aliases; exact-only versus recursive targets; and payload-versus-non-payload identity. Rule repository-backed/floating-source payloads explicitly: update the resolved source under the existing local-access policy, materialize a mesh-local working file and change inventory, or refuse with a typed unsupported-source error. This ruling must be validated against the first srd-rules consumer shape rather than assumed.

7. **PROPOSED HIGH — The “stable public surface” has neither exact symbols nor a stability boundary.** The draft correctly observes wholesale reachability: root `src/mod.ts` re-exports core, runtime, and CLI (`src/mod.ts:1-3`), whose module barrels recursively export payload and weave symbols (`src/core/mod.ts:1-10`, `src/runtime/mod.ts:1-10`). `executeVersion` is already a named reachable function; what is missing is a deliberately supported bytes-in contract. “`src/api/` (or `core/api/`)” leaves architecture to the builder, but `core/api/` would violate the repository law that core avoids filesystem/runtime assumptions (`documentation/notes/wd.codebase-overview.md:15-19`). No function/type/error names, import example, experimental marker, or exact-versus-latitude mark is provided. **REQUIRED RESOLUTION:** Rule `src/api/` (not `core/api/`) for the filesystem-owning façade; pin the v1 function, request, result, target-result, and error export names and their root import path; state which are stable and which existing wholesale exports remain incidental; and include a compiling normative import/call example. If the PM deliberately leaves naming latitude to build, mark every such identifier LATITUDE and require adjudication before merge, not silent invention.

8. **PROPOSED HIGH — The promised result and typed refusal set are not derivable from the proposed “natural seeds.”** `VersionPlan` carries mesh base, versioned designator paths, and planned file contents, but no per-target payload IRI, chosen history/state/manifestation, current-history path, applied/no-op status, or refusal (`src/core/weave/version_plan.ts:3-9`). `PayloadVersionIntentPlan` has payload IRI/current history and optional next-state hint, but it describes the separate inventory-intent operations and not an applied version (`src/core/payload/version_intent.ts:33-40`, `src/core/payload/version_intent.ts:49-145`). Runtime `VersionResult` exposes paths only (`src/runtime/weave/weave.ts:94-99`). Existing failures span input, runtime, config, path-access, snapshot-conflict, create-collision, and raw I/O domains; the CLI reduces them to message text (`src/cli/run.ts:1967-1993`). The note leaves thrown errors versus result unions open and gives no stable machine code or partial-write representation. **REQUIRED RESOLUTION:** Specify a per-target success algebra (`applied`/`alreadyCurrent`, resolved identity, selected history/state/manifestation and snapshot path), batch-created/updated working and version paths, and whether the full mutable-content `VersionPlan` is exposed. Specify stable refusal/error codes and fields at minimum for invalid request/identity, target not found/not payload, malformed mesh/inventory/config, inconsistent batch policy, unsupported source/content, snapshot conflict, validation/plan conflict, path/permission failure, and write failure with phase/possible partial paths. Rule throw-versus-return and make message text diagnostic, not the machine contract.

9. **PROPOSED HIGH — Configuration/default precedence is described as “mirror the CLI” but is not restatable from the note.** CLI-level general segment flags are copied onto targets only when the target omitted that field (`src/cli/run.ts:1703-1777`). Runtime then loads mesh and ancestor/target-scoped effective config (`src/runtime/weave/execution_config.ts:70-138`, `src/runtime/weave/execution_config.ts:205-245`), and a batch refuses inconsistent target-scoped planning policies (`src/runtime/weave/version_execution.ts:734-780`). Layout resolution uses explicit target history over current intent/default policy and explicit state over `hasNextStateSegmentHint`/policy (`src/core/weave/payload_version_layout.ts:45-97`, `src/core/weave/payload_version_layout.ts:100-240`). `historyTrackingPolicyOverride` and `overwriteExistingState` are also existing version controls (`src/runtime/weave/weave.ts:48-54`, `src/core/weave/requests.ts:16-19`) but are not integrated into the proposed options contract. Current snapshot hashing explicitly excludes support/config inputs (`documentation/notes/wd.decision-log.md:32-39`). **REQUIRED RESOLUTION:** Write an ordered precedence table for every option: per-item segments, batch defaults, persisted payload intent/current history, target/ancestor/mesh config naming policy, built-in default, history-tracking override, and overwrite. State that target-scoped policy disagreement refuses the batch, and whether API batch defaults are semantically identical to CLI general flags. Explicitly preserve or strengthen the current exclusion of config/support files from the capture hash; do not imply they are verified today.

10. **PROPOSED HIGH — “Fail-closed validation” is not scoped to an actual validation operation.** `executeVersion` normalizes, prepares, and validates the planned RDF files before writing (`src/runtime/weave/weave.ts:217-251`, `src/runtime/weave/version_execution.ts:936-994`); it does not run whole-mesh validation. Whole-mesh pre/post validation flags exist on composed `weave`, not `weave version` (`src/cli/run.ts:155-161`, `src/cli/run.ts:291-375`). The bytes API could mean byte/content validation, existing-shape validation, plan RDF parsing, whole-mesh preflight, or all of them. **REQUIRED RESOLUTION:** Enumerate the mandatory validation stages and their read/write boundaries. State content-kind behavior (especially RDF versus opaque binary), whether whole-mesh validation is absent/optional/mandatory, whether working bytes are validated before installation, and which validation refusal code names each stage.

11. **PROPOSED HIGH — The test plan omits load-bearing negative families and one positive boundary.** It names malformed target, mid-capture change, malformed member, no-op, and one binary round-trip, but not: one-member retry; duplicate/aliased target identities; empty batch; recursive or mixed payload/non-payload requests; inconsistent target-scoped policies; named-state omission, duplicate/existing requested state, missing/conflicting/invalid progression facts; malformed/unknown options; outside-mesh IRI; missing/non-file/out-of-policy working paths; repository-floating sources; malformed mesh metadata/inventory/config; command/batch/per-target/config precedence; caller mutation of the same `Uint8Array`; shared buffer/view offsets; zero-length bytes; invalid UTF-8 and extension/content-kind mismatch; later binary advancement; concurrent API/API and API/CLI at each capture/write boundary; mutation after verified capture (which landed behavior intentionally ignores); config/support mutation outside the hash set; create/update collisions; and partial write failures/retry. The landed post-capture boundary is explicitly tested (`tests/integration/weave_test.ts:2121-2173`) and should be part of equivalence. **REQUIRED RESOLUTION:** Add these families or explicitly mark each out of contract with rationale. Separate unit contract tests, real-filesystem integration tests, CLI/API equivalence tests, and an `sf.spec.*`/Accord acceptance transition. “Fail-on-old” must identify the old behavior each probe defeats rather than remain a slogan.

12. **PROPOSED MEDIUM — Scope fences are mostly honest, but two goals silently pierce them.** Press-side consumption is cleanly excluded by Goals/Consumers, Testing, and Non-Goals; none of the API deliverables requires Stagecraft wiring. The named 0.3.0 defect trio is unrelated and should stay excluded. However, enforced API/CLI single-writer coordination would necessarily change shared CLI runtime behavior (finding 2), and promised later binary recording exposes a directly adjacent binary-version defect (finding 3). Those are not any of the fenced defect trio, but they cannot remain unowned while the goals promise their outcomes. **REQUIRED RESOLUTION:** After adjudicating findings 2 and 3, amend Goals, Contract Changes, and Non-Goals consistently: either own the shared lock/binary correctness work, or narrow the API guarantees. Keep all press-side code/tests and the three named 0.3.0 defects out.

13. **PROPOSED MEDIUM — An `sf.spec.*` fast-follow would violate the repository's spec-first rule for this unsettled behavior.** The operation is external, cross-subsystem, invariant-heavy, and integration-test-shaped, meeting every trigger in [[wd.general-guidance#behavior specs]] and [[wd.testing#spec-driven testing]]. `wd.testing` says to refine the behavior spec first when expected behavior is unclear (`documentation/notes/wd.testing.md:15-25`, `documentation/notes/wd.testing.md:125-168`), which is exactly the state of findings 1-5. **REQUIRED RESOLUTION:** Make an `sf.spec.*` note in `semantic-flow-framework` a pre-build gate in this task, after PM adjudication fixes the operation boundary but before implementation tests. The portable spec should cover bytes-supplied exact payload recording, batch ordering/coherence, working/current/historical observable state, config/segment precedence, refusal-before-version-output, snapshot/capture boundary, and idempotent retry. Keep Deno symbol names, `Uint8Array`, filesystem lock mechanics, local path policy, error class names, and logging in a Weave `wd.*` API contract. The spec and tests land together; it must not be a post-close fast-follow.

14. **PROPOSED LOW — Release/stability delivery is unresolved.** Root version is currently `0.3.0` (`deno.json:1-3`), and the release runbook makes root version and release notes authored release inputs (`documentation/notes/wd.release-runbook.md:15-38`). A “stable” consumer surface without a ruled version/import vehicle leaves the first consumer pinning an unspecified commit despite the product-path goal. **REQUIRED RESOLUTION:** Rule whether this task prepares 0.4.0 or lands source-only for a later release. If “stable” is intended on task close, prefer the pre-1.0 minor bump to 0.4.0 and its release note; actual publication remains governed by the release runbook and need not be performed by this task unless separately authorized.

#### Landed-reality audit

- **Verified:** The CLI has the claimed two-command workflow, with `payload update` and `version` independently routed through runtime (`src/cli/run.ts:1100-1154`, `src/cli/run.ts:291-375`). It is not an atomic two-phase command.
- **Verified:** `src/mod.ts` does wholesale re-export core, runtime, and CLI (`src/mod.ts:1-3`), and the nested barrels make the named planner/runtime symbols reachable. Reachability is not itself a documented stability promise.
- **Contradicted/qualified:** Single-writer behavior lives with the requesting application, not the CLI/runtime (finding 2).
- **Contradicted/qualified:** `VersionPlan` and `PayloadVersionIntentPlan` do not collectively define the promised applied per-target result (finding 8).
- **Contradicted/qualified:** `PlannedBinaryFile` is a created-output carrier, not evidence that payload update or later payload advancement is byte-safe (finding 3).
- **Contradicted/qualified:** Whole-plan refusal, snapshot verification, and already-current no-op are special explicit-batch semantics, not blanket properties of every version invocation (findings 4-5).

#### Open-issue lean verdicts

- **API shape — REFUTE the composable pair as the primary v1 contract; ENDORSE a composed coherent-batch call as primary.** The pair reproduces the CLI deployment seam inside one process but cannot by itself give bytes-in batch validation, capture, or refusal semantics across the update/version gap. A lower-level `updatePayloadBytes` and version-current call may exist for human-equivalent workflows only if their weaker boundary is explicit. The first consumer needs one request that accepts all bytes/identities, captures them, plans the coherent batch, and returns per-target applied/no-op outcomes. The thin convenience should not be the only call that owns the strongest guarantees.
- **Batch surface — ENDORSE multi-target v1, with conditions.** The first consumer is inherently multi-target and the landed deterministic canonical ordering/merged support progression is the correct semantic base. Define the API request as a coherent batch even at cardinality one, reject duplicates after identity normalization, support exact payload targets only, and call the guarantee “whole-plan pre-write refusal,” not filesystem transactionality. Decide separately whether update-only accepts batches.
- **Naming/segments — ENDORSE one options object only after an explicit precedence table is added.** Use batch defaults plus per-item overrides analogous to CLI general flags and target fields, but also include persisted intent, target-scoped config, built-in policy, `historyTrackingPolicyOverride`, and overwrite behavior. “Mirror exactly” is not an executable rule by itself.
- **Error taxonomy — ENDORSE typed thrown errors, not message matching, based on the existing runtime convention.** Prefer one stable public error base with discriminated `code`, `stage`, optional target/path/batch details, and `cause`; subclasses are optional. Applied/no-op is a normal result, not an error. Keep operational I/O failure distinct from semantic refusal and disclose possible partial paths for write-stage failures.
- **Stable-surface boundary — ENDORSE dedicated `src/api/`; REFUTE `core/api/`.** The façade must perform filesystem/config/runtime orchestration and therefore does not belong in pure core. Add explicit API exports and document only those names as stable even while older wholesale exports remain reachable. Pin exact names/imports before merge.
- **Binary payloads — ENDORSE `Uint8Array` as the only payload-byte representation, conditional on owning the end-to-end fix.** Copy inputs at admission and preserve exact bytes. Do not add a string overload in v1. If the adjacent later-binary path is not owned, refute binary scope for this release rather than claim a round-trip the CLI/runtime cannot supply.
- **`sf.spec.*` — RULE AS A GATE, not a fast-follow.** Land it in `semantic-flow-framework`; cover portable observable operation behavior there and Weave/Deno mechanics in a new `wd.*` API contract, as detailed in finding 13.
- **Version/release — ENDORSE 0.4.0 if this task closes with the API called stable.** This is a new pre-1.0 public capability and fits a minor increment. A source-only unreleased landing is possible only if the note stops calling the surface consumably stable and records the later release dependency.

### r1 proposed verdict

**PROPOSED: BLOCKED ON SPEC.** Minimum before a build seat starts: (1) rule the primary composed operation and any weaker companion calls, with a precise bytes-copy/capture/validation/write boundary; (2) rule caller-owned concurrency versus a shared API/CLI lock; (3) rule binary inclusion and the adjacent later-binary correction; (4) define one-member and multi-member no-op/batch semantics; (5) pin identity/source handling, option precedence, public export names, per-target results, and stable error codes; and (6) approve the portable `sf.spec.*` behavior note as the test-driving gate. Release number may be finalized before close, but not left unresolved when the surface is declared stable.

### r1 adjudication + RESOLVED SPEC (amendment r1 — PM, 2026-07-21 13:38; GOVERNS wherever it conflicts with the body)

**Verdict SUSTAINED: BLOCKED ON SPEC was right, and the review corrected the adjudicator twice** — the composable-pair lean (refuted with the update/version commit-boundary evidence) and the binary promise (refuted with the later-binary text-decode evidence at `src/core/weave/weave.ts:1682-1760`). Both reversals are owned here in writing. Dispositions, then the resolved spec:

1. **F1 → CONCUR.** The primary operation is ruled below with a full phase contract.
2. **F2 → CONCUR, ruling (a): CALLER-OWNED single-writer stands** (the landed decision-log law). Concurrent API/API and API/CLI mutation of one mesh is OUTSIDE the supported contract; the bounded snapshot guard is a hazard reducer, not enforcement; all "guarantee/enforcement" language falls. No shared-lock work — that would rewrite landed CLI/runtime policy this task fences out.
3. **F3 → CONCUR, ruling: v1 NARROWS to the UTF-8 text/RDF payload class.** `Uint8Array` remains the only boundary representation (admission-copied, UTF-8-validated; invalid UTF-8 and non-text content-kinds refuse typed). The binary round-trip promise is WITHDRAWN from v1; the adjacent later-binary runtime defect (decoded-text writes on later advancement) is recorded in [[wd.todo]] as a named follow-up with this finding as evidence. Binary lands as its own task owning the end-to-end fix.
4. **F4 → CONCUR.** Every API request IS a coherent batch, including cardinality one: one-member requests get the identical snapshot/no-op/refusal semantics. The API path always drives the explicit-batch machinery; if the landed runtime restricts that path to >1 members, the API lifts that restriction FOR THE API PATH ONLY (no CLI behavior change). Duplicates after normalization refuse; empty batches refuse; exact payload targets only (no recursive) in v1; `overwriteExistingState` is an explicit option.
5. **F5 → CONCUR.** Three guarantees, stated separately in the phase contract below: whole-request semantic refusal BEFORE any write; working+version writes after plan-green; non-transactional I/O failure disclosed with phase + possible partial paths in the typed error. Write-failure injection tests at each write phase join the plan. "All-or-nothing" language falls.
6. **F6 → CONCUR, ruling: designator-path-only identity in v1.** Payload-IRI-in is dropped (the RESULT carries the resolved payload IRI); `meshRoot` is a required explicit absolute path (no cwd default — fail closed); `/` root designator supported as landed; duplicate identities after normalization refuse. Repository-backed/floating-source payloads REFUSE with a typed unsupported-source error in v1 — validated against the first consumer: the srd-rules pipeline's payloads are all mesh-local working files.
7. **F7 → CONCUR, ruling: `src/api/`** (core purity law honored). Pinned names: `versionPayloads` (exact), `VersionPayloadsRequest` (exact), `VersionPayloadsResult` (exact), error base `WeaveApiError` (exact) with `code` and `stage` discriminant fields (exact); per-target outcome type `PayloadVersionOutcome` (LATITUDE); refusal-code VALUES enumerated below with spelling LATITUDE, adjudicated before merge. A compiling normative import/call example is a before-code deliverable (the wd API contract note).
8. **F8 → CONCUR; the review's result algebra is ADOPTED:** per-target `applied` | `alreadyCurrent` with resolved identity (designatorPath, payloadArtifactIri, historySegment, stateSegment, manifestationSegment, snapshot path); request-level created/updated path lists; the mutable `VersionPlan` is NOT exposed. Errors are TYPED THROWN: one public base, discriminated `code` + `stage`, optional target/path/batch detail, `cause` chained; message text is diagnostic, never the machine contract. Minimum code families: invalid-request, unknown-target, not-a-payload, malformed-mesh, inconsistent-policy, unsupported-source, unsupported-content, snapshot-conflict, plan-conflict, io-failure (with phase + partial paths).
9. **F9 → CONCUR.** The wd API contract note MUST carry the ordered precedence table: per-item explicit segments → batch defaults → persisted payload intent (current history / next-state hint) → target/ancestor/mesh config naming policy → built-in defaults; `historyTrackingPolicyOverride` and `overwriteExistingState` as explicit options; target-scoped policy disagreement refuses the batch; the capture-hash EXCLUSION of config/support files is preserved and stated (never implied as verified).
10. **F10 → CONCUR.** Validation stages enumerated: (i) request/identity validation; (ii) UTF-8/content-kind validation at admission; (iii) settled-shape/inventory/config validation at load; (iv) plan RDF parse/preflight before writes. Whole-mesh validation is ABSENT from this API by design (as landed `version`); each stage names its refusal code.
11. **F11 → CONCUR.** The enumerated families are adopted into Testing, each marked in-contract; OUT with rationale: later-binary advancement (v1 narrowed, F3), broad concurrent-interleaving matrices (caller-owned law, F2 — ONE snapshot-guard probe retained, equivalent to the landed `tests/integration/weave_test.ts:2121-2173` boundary), recursive targets (exact-only v1). Fail-on-old probes name the exact old behavior each defeats.
12. **F12 → CONCUR.** Goals/Contract Changes/Non-Goals are amended by this section per F2(a) and F3; the fences hold: no lock work, no binary, no press wiring, no 0.3.0 defect trio.
13. **F13 → CONCUR: the `sf.spec.*` note is a PRE-BUILD GATE**, landed in `semantic-flow-framework` (portable observable behavior: bytes-supplied exact payload recording, batch coherence, working/current/historical observable state, precedence, refusal-before-version-output, capture boundary, idempotent retry). A `wd.*` API contract note in the weave vault carries Deno mechanics (names, `Uint8Array`, paths, error classes, the precedence table, the compiling example). BOTH are the build seat's BEFORE-CODE deliverables with a hard STOP for PM sign-off before implementation — spec-first honored, one seat holds context (the proven srd-rules pattern).
14. **F14 → CONCUR: this task prepares 0.4.0** (root bump + release note authored in-task per the runbook); actual publication stays runbook-governed and is not this task's push.

**THE RESOLVED OPERATION (the phase contract, governing):** `versionPayloads(request)` — one composed coherent-batch call. Request: `{ meshRoot (absolute, required); items: [{ designatorPath, bytes: Uint8Array, historySegment?, stateSegment?, manifestationSegment? }]; defaults?; historyTrackingPolicyOverride?; overwriteExistingState? }`. Phases: **ADMIT** (copy every buffer — caller mutation after call start is invisible; UTF-8/content-kind validate; normalize identities, refuse duplicates/empty) → **LOAD** (mesh/inventory/config validation) → **PLAN from an in-memory byte overlay** — NO disk mutation of any kind before the whole-request plan validates; any member's failure refuses the ENTIRE request with the mesh untouched → **WRITE** (working files + version outputs; I/O failure here is non-transactional, disclosed with phase + partial paths) → **RESULT** (per-target applied/alreadyCurrent + resolved identity). Post-plan disk mutation by another writer is the unsupported-concurrency case (F2). The CLI's changed-under-capture refusal has NO direct bytes-in analogue — the admitted copies are authoritative; the amendment says so rather than pretending the old guarantee transfers.

**Status: CONVERGENCE CHECK r2 fires now (light, on this amendment). At its green: the build seat fires with the two before-code spec notes + the batch-cardinality-one confirmation as STOP-gated deliverables → PM GO → implementation.**

## Receipts

*(build seat appends; append-only; anything skipped is named.)*
